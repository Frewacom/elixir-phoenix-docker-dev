{
    "scripts": {
        "docker:shell": "docker-compose exec web sh",
        "docker:dev": "docker-compose up",
        "docker:migrate": "docker-compose run web mix ecto.migrate",
        "tsc:watch": "./assets/node_modules/typescript/bin/tsc -W -p ./assets/tsconfig.json"
    }
}
